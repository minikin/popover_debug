name: popover_playground
description: Popover Playground.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  popover: ^0.2.6+1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
